---
description: Chinitsu Showdown 项目上下文、命令与进度跟踪约定
alwaysApply: true
---

# Chinitsu Showdown（清一色双人麻将）

## 进度来源（必读）

进行**实质性开发或规划**前，先读仓库根目录旁的 **`.cursor/PROJECT_STATUS.md`**，按其中「当前阶段 / 进行中 / 下一步」行动。完成可交付里程碑后，**在同一轮对话里更新该文件**（日期、阶段、勾选项），便于后续 chat 接续。

## 权威文档（按需打开）

| 用途 | 路径 |
|------|------|
| 命令与架构 | `CLAUDE.md`（根目录）、`server/CLAUDE.md` |
| 游戏规则 | `RULES.md` |
| 用户向说明与 WS 说明 | `README.md` |
| 协议变更 | 同步改 `docs/asyncapi.yaml` 与 `docs/asyncapi.zh.yaml` |

## 约定

- 后端：`server/`（`game.py` 引擎；`replay_recorder.py` 谱面录制、`replay_codec.py` 可选精简格式；`managers.py` 房间/WS；`replay.py` 重放帧；`agari_judge.py` 和牌）。
- 前端：`web-svelte/`（`ws.ts`、`types.ts`；复盘 `routes/replay/`、`ReplayViewer.svelte`、`replayView.ts`）。
- 用 **8000 静态页** 测 UI 时：改前端后必须 **`npm run build`**（输出到 `web-svelte/build/`）。
- 依赖：`uv sync`；测试：根目录 `uv run pytest -v`。
- 勿在规则里重复长文；细节以 `PROJECT_STATUS.md` 与 `CLAUDE.md` 为准。
